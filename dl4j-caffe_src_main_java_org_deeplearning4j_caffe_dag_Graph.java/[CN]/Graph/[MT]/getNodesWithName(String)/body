{
  List<Node> matchedNodeList=new ArrayList<>();
  for (  Node node : adjacencyListMap.keySet()) {
    if (node.getName().equals(nameOfNode))     matchedNodeList.add(node);
  }
  return matchedNodeList;
}
