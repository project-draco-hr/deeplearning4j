{
  String s="Connections:\n";
  for (int i=0; i < numnodes; i++) {
    s+=i + ": ";
    for (int j=0; j < maxedges; j++) {
      if (edges[i][j] != -1) {
        s+=edges[i][j] + ",";
      }
    }
    s+="\n";
  }
  return s;
}
