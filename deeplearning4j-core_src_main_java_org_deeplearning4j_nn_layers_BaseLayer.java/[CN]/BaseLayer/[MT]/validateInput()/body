{
  if (conf.getBatchSize() > 0) {
    if (input.rows() != conf.getBatchSize())     throw new IllegalStateException("Illegal batch size " + input.rows() + " should have been "+ conf.getBatchSize());
  }
}
