{
  if (conf.getDropOut() > 0) {
    this.dropoutMask=Nd4j.rand(input.rows(),conf.getnOut()).gt(conf.getDropOut());
  }
 else   this.dropoutMask=Nd4j.ones(input.rows(),conf.getnOut());
  input.muli(dropoutMask);
}
