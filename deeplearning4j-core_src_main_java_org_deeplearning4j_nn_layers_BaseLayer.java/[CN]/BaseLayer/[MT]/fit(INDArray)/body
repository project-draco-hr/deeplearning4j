{
  if (input != null)   this.input=input;
  Solver solver=new Solver.Builder().model(this).configure(conf()).listeners(conf.getListeners()).build();
  this.optimizer=solver.getOptimizer();
  solver.optimize();
}
