{
  if (iter == null)   return (passes < numPasses) && ((batchedDS.size() != 0 && batchedDS.size() > batch) || batchedDS.size() == 0);
 else   return (passes + 1 < numPasses) || (iter.hasNext() && passes == 0);
}
