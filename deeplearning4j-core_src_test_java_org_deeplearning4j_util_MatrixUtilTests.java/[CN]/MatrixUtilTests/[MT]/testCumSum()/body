{
  DoubleMatrix test=new DoubleMatrix(new double[][]{{1,2,3},{4,5,6}});
  DoubleMatrix cumSum=MatrixUtil.cumsum(test);
  DoubleMatrix solution=new DoubleMatrix(new double[][]{{1,2,3},{5,7,9}});
  assertEquals(solution,cumSum);
}
