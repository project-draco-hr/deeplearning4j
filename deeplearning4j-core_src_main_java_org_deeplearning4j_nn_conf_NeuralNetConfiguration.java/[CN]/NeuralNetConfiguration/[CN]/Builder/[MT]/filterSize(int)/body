{
  if (filterSize == null)   return this;
  if (filterSize == null || filterSize.length != 4)   throw new IllegalArgumentException("Invalid filter size must be length 2");
  this.filterSize=filterSize;
  return this;
}
