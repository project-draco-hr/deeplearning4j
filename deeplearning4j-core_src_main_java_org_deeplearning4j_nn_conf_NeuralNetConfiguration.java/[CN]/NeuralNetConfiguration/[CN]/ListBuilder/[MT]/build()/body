{
  List<NeuralNetConfiguration> list=new ArrayList<>();
  for (int i=0; i < layerwise.size(); i++)   list.add(layerwise.get(i).build());
  MultiLayerConfiguration ret=new MultiLayerConfiguration.Builder().hiddenLayerSizes(hiddenLayerSizes).confs(list).build();
  return ret;
}
