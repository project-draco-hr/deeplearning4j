{
  List<INDArray> ret=new ArrayList<>();
  for (  String s : order) {
    if (!gradientForVariable().containsKey(s))     continue;
    ret.add(gradientForVariable().get(s));
  }
  return Nd4j.toFlattened(ret);
}
