{
  if (x != null)   this.input=x;
  this.lastMiniBatchSize=x.rows;
  optimizer=new DenoisingAutoEncoderOptimizer(this,lr,new Object[]{corruptionLevel,lr},optimizationAlgo,lossFunction);
  optimizer.train(x);
}
