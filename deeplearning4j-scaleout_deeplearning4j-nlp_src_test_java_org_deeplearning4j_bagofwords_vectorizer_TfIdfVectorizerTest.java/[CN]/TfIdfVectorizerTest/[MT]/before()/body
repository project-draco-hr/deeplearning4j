{
  FileUtils.deleteDirectory(new File("tfidf"));
  cache=new InMemoryLookupCache();
  index=new LuceneInvertedIndex.Builder().cache(cache).indexDir(new File("tfidf")).batchSize(5).cacheInRam(false).build();
}
