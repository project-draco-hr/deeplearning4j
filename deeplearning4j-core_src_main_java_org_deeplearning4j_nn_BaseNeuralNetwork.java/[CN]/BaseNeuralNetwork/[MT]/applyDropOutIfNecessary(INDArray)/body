{
  if (conf.getDropOut() > 0) {
    this.doMask=NDArrays.rand(input.rows(),input.columns()).gt(conf.getDropOut());
  }
 else   this.doMask=NDArrays.ones(input.rows(),input.columns());
  input.muli(doMask);
}
