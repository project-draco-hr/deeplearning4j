{
  MultiLayerConfiguration conf=new NeuralNetConfiguration.Builder().layer(new RBM()).nIn(2).nOut(1).weightInit(WeightInit.DISTRIBUTION).dist(new NormalDistribution(0,1)).seed(12345l).list(2).hiddenLayerSizes(5).override(1,new ClassifierOverride()).build();
  return conf;
}
