{
  Tensor h1=(Tensor)h;
  for (int i=0; i < numFilters; i++) {
    visI.setSlice(i,convolution2D(h1.getSlice(i),W.getSlice(i)));
  }
  DoubleMatrix I=visI.sliceElementSums().addRowVector(vBias);
  return sigmoid(I);
}
