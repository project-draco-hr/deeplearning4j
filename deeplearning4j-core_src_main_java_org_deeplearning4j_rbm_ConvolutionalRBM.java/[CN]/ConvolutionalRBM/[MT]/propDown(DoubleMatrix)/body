{
  Tensor h1=(Tensor)h;
  for (int i=0; i < numFilters; i++) {
    visI.setSlice(i,Convolution.conv2d(h1.getSlice(i),W.getSlice(i),Convolution.Type.FULL));
  }
  DoubleMatrix I=visI.sliceElementSums().addRowVector(vBias);
  return sigmoid(I);
}
