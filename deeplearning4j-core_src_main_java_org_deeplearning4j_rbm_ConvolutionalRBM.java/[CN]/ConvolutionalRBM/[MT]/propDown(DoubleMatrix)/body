{
  Tensor h1=(Tensor)h;
  for (int i=0; i < numFilters; i++) {
    visI.setSlice(i,MatrixUtil.padWithZeros(Convolution.conv2d(h1.getSlice(i),W.getSlice(i),Convolution.Type.FULL),visI.rows(),visI.columns()));
  }
  return new Tensor(sigmoid(visI.sliceElementSums().addRowVector(vBias)));
}
