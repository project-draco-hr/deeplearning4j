{
  if (d1.abs() > d2.abs())   return 1;
 else   if (d2.abs() > d1.abs())   return -1;
 else {
    if (angle(d1) > angle(d2))     return 1;
 else     if (angle(d1) < angle(d2))     return -1;
    return 0;
  }
}
