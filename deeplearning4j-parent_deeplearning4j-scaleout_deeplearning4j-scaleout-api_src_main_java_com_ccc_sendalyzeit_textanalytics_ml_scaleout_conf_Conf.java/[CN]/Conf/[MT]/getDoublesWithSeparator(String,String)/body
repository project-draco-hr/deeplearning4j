{
  String get=get(key);
  if (get != null) {
    String[] split=get.split(separator);
    double[] ret=new double[split.length];
    for (int i=0; i < split.length; i++)     ret[i]=Double.parseDouble(split[i]);
    return ret;
  }
  return null;
}
