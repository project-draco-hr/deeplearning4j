{
  String get=get(key);
  if (get != null) {
    get=get.trim();
    String[] split=get.split(separator);
    int[] ret=new int[split.length];
    for (int i=0; i < split.length; i++)     ret[i]=Integer.parseInt(split[i]);
    return ret;
  }
  return null;
}
