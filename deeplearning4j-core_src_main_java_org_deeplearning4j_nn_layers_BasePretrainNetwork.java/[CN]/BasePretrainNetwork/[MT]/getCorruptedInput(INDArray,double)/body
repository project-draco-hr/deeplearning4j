{
  INDArray corrupted=Nd4j.zeros(x.shape());
  INDArray linear=corrupted.linearView();
  for (int i=0; i < x.length(); i++)   linear.putScalar(i,Nd4j.getDistributions().createBinomial(1,1 - corruptionLevel).sample());
  corrupted.muli(x);
  return corrupted;
}
