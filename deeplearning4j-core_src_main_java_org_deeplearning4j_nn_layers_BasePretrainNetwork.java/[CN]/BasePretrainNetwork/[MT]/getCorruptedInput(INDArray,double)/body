{
  INDArray corrupted=Nd4j.zeros(x.shape());
  INDArray linear=corrupted.linearView();
  for (int i=0; i < x.length(); i++)   linear.putScalar(i,binomial(conf.getRng(),1,1 - corruptionLevel));
  corrupted.muli(x);
  return corrupted;
}
