{
  if (shape().length > 1)   throw new IllegalArgumentException("Unable to do linear indexing with dimensions greater than 1");
  int realStride=stride == null || stride.length < 1 ? 1 : stride[0];
  return data[offset + i * realStride];
}
