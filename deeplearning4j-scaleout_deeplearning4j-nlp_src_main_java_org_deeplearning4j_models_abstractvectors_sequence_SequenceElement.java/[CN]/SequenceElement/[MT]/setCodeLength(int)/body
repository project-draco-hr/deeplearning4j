{
  this.codeLength=codeLength;
  if (codes.size() < codeLength) {
    for (int i=codes.size(); i < codeLength; i++)     codes.add(0);
  }
  if (points.size() < codeLength) {
    for (int i=codes.size(); i < codeLength; i++)     points.add(0);
  }
}
