{
  StringBuffer sb=new StringBuffer();
  char c;
  for (int i=0; i < orig.length(); i++) {
    c=orig.charAt(i);
    if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z') || (c >= '0' && c <= '9')) {
      sb.append(c);
    }
  }
  return sb.toString();
}
