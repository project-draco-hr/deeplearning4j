{
  cache=new InMemoryLookupCache.Builder().vectorLength(100).build();
  index=new LuceneInvertedIndex.Builder().indexDir(new File("bagofwords")).cache(cache).batchSize(5).cacheInRam(false).build();
}
