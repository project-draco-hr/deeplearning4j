{
  Tsne tsne=new Tsne();
  MnistDataFetcher fetcher=new MnistDataFetcher(true);
  fetcher.fetch(10);
  DataSet d2=fetcher.next();
  List<String> list=new ArrayList<>();
  for (int i=0; i < list.size(); i++)   list.add(String.valueOf(i));
  tsne.plot(d2.getFeatureMatrix(),2,0.2f,28,list);
}
