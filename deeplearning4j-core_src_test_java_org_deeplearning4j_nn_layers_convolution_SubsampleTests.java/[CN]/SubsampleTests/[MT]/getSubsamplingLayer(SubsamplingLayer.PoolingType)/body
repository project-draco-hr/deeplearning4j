{
  NeuralNetConfiguration conf=new NeuralNetConfiguration.Builder().activationFunction("relu").constrainGradientToUnitNorm(true).seed(123).nIn(1).nOut(20).layer(new SubsamplingLayer.Builder(pooling).build()).build();
  return LayerFactories.getFactory(new SubsamplingLayer()).create(conf);
}
