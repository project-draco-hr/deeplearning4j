{
  Layer model=getSubsamplingLayer();
  Gradient gradient=createPrevGradient(1,3,20);
  Pair<Gradient,INDArray> out=model.backpropGradient(null,gradient,null);
  assertEquals(out.getSecond().shape()[0],3);
}
