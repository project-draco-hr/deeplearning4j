{
  this.sentenceIterator=sentenceIterator;
  if (!this.sentenceIterator.hasNext())   this.sentenceIterator.reset();
  this.tokenizerFactory=tokenizerFactory;
  this.labels=labels;
  this.numTop=numTop;
  stopWords=StopWords.getStopWords();
}
