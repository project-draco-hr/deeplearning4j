{
  bootstrap.addBundle(new ViewBundle<ApiConfiguration>(){
    @Override public ImmutableMap<String,ImmutableMap<String,String>> getViewConfiguration(    ApiConfiguration arg0){
      return ImmutableMap.of();
    }
  }
);
}
