{
  try {
    lock.readLock().lock();
    if (current == null) {
      updated=false;
      return Response.ok(new HashMap<>()).build();
    }
    updated=false;
    return Response.ok(current).build();
  }
  finally {
    lock.readLock().unlock();
  }
}
