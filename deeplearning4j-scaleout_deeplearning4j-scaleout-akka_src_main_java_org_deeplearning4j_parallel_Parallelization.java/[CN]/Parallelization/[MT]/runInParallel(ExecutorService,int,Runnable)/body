{
  for (int i=0; i < numWorkers; i++)   exec.execute(runnable);
  exec.shutdown();
  try {
    exec.awaitTermination(1,TimeUnit.DAYS);
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
}
