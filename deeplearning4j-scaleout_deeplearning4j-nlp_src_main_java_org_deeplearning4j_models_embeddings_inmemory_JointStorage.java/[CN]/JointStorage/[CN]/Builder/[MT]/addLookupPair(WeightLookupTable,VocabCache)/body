{
  if (lookupTable.getTableId() == null || lookupTable.getTableId().longValue() == 0)   lookupTable.setTableId(RandomUtils.nextLong());
  for (  SequenceElement word : cache.vocabWords()) {
    word.setStorageId(lookupTable.getTableId());
  }
  mapTables.put(lookupTable.getTableId(),lookupTable);
  mapVocabs.put(lookupTable.getTableId(),cache);
  return this;
}
