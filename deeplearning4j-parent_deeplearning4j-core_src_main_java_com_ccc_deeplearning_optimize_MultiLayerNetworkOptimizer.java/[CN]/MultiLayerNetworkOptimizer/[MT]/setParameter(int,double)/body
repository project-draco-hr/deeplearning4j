{
  if (index >= network.getLogLayer().getW().length) {
    int i=index - network.getLogLayer().getB().length;
    network.getLogLayer().getB().put(i,value);
  }
 else   network.getLogLayer().getW().put(index,value);
}
