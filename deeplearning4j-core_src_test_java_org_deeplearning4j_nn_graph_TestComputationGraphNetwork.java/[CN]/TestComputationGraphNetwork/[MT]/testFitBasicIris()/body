{
  ComputationGraphConfiguration configuration=new NeuralNetConfiguration.Builder().graphBuilder().addInput(0,"input").addLayer("firstLayer",new DenseLayer.Builder().nIn(4).nOut(5).build(),"input").addLayer("outputLayer",new OutputLayer.Builder().nIn(5).nOut(3).build(),"firstLayer").build();
  ComputationGraph graph=new ComputationGraph(configuration);
  graph.init();
  DataSetIterator iris=new IrisDataSetIterator(150,150);
  graph.fit(iris);
}
