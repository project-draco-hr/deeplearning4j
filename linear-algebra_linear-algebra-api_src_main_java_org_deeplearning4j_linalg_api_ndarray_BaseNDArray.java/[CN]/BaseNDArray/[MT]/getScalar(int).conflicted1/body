{
  int ix=offset;
  for (int i=0; i < shape.length; i++) {
    ix+=indexes[i] * stride[i];
  }
  if (ix >= data.length)   throw new IllegalArgumentException("Illegal index " + Arrays.toString(indexes));
  return NDArrays.scalar(data[ix]);
}
