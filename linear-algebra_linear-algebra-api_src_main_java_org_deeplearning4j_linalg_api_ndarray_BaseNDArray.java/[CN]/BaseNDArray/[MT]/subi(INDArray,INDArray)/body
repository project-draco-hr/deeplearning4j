{
  if (other.isScalar())   new TwoArrayOps().from(this).scalar(other.element()).op(SubtractOp.class).to(result).build().exec();
 else   new TwoArrayOps().from(this).other(other).op(SubtractOp.class).to(result).build().exec();
  return result;
}
