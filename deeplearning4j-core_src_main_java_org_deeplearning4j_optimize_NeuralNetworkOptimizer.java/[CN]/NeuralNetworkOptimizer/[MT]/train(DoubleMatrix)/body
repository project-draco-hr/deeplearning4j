{
  if (opt == null)   opt=new VectorizedNonZeroStoppingConjugateGradient(this,this);
  opt.setTolerance(tolerance);
  int epochs=extraParams.length < 3 ? 1000 : (int)extraParams[2];
  opt.optimize(epochs);
}
