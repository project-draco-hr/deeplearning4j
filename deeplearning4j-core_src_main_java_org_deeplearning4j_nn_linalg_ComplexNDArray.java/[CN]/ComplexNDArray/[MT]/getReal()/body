{
  int[] stride=ArrayUtil.copy(stride());
  for (int i=0; i < stride.length; i++)   stride[i]/=2;
  NDArray result=new NDArray(shape(),stride);
  NativeBlas.dcopy(length,data,offset,2,result.data,0,1);
  return result;
}
