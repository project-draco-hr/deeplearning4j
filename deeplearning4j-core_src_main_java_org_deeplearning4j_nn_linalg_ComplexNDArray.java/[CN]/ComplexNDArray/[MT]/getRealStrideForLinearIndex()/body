{
  if (stride.length != shape.length)   throw new IllegalStateException("Stride and shape not equal length");
  if (shape.length == 1)   return stride[0];
  if (shape.length == 2) {
    if (shape[0] == 1)     return stride[1];
    if (shape[1] == 1)     return stride[0];
  }
  return stride[0];
}
