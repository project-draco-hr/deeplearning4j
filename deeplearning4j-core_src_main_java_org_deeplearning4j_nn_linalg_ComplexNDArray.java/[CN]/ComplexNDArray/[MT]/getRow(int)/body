{
  if (shape.length == 2)   return new ComplexNDArray(data,new int[]{shape[1]},new int[]{stride[1]},offset + (4 * r));
 else   throw new IllegalArgumentException("Unable to get row of non 2d matrix");
}
