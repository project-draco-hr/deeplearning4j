{
  int realStride=getRealStrideForLinearIndex();
  int idx=offset + (2 * i) * realStride;
  if (idx >= data.length)   throw new IllegalArgumentException("Illegal index " + idx + " derived from "+ i);
  return idx;
}
