{
  List<Integer> list=new ArrayList<>();
  for (int i=from; i < from + num + 1; i++) {
    list.add(i);
    list.add(i + 1);
  }
  return new DimensionSlice(false,new ComplexNDArray(data,new int[]{2},new int[]{1}),ArrayUtil.toArray(list));
}
