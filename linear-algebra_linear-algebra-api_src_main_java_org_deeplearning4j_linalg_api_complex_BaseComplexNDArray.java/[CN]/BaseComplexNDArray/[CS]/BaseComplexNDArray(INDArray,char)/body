{
  this(m.shape(),ordering);
  INDArray flattened=m.reshape(new int[]{1,m.length()});
  IComplexNDArray flatten=reshape(1,length);
  for (int i=0; i < length; i++) {
    flatten.putScalar(i,flattened.get(i));
  }
}
