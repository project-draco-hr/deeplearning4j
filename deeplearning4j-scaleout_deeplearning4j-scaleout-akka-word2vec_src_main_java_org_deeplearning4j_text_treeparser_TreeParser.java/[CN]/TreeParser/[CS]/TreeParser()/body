{
  if (parser == null) {
    parser=getParser();
  }
  if (tokenizer == null)   tokenizer=getTokenizer();
  if (pool == null)   pool=new CasPool(Runtime.getRuntime().availableProcessors() * 10,parser);
}
