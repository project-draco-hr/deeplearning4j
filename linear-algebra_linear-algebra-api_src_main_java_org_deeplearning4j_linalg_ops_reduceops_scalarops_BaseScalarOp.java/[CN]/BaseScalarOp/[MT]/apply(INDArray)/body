{
  INDArray doNDArray=input.isVector() ? input : input.linearView();
  float start=startingValue;
  for (int i=0; i < doNDArray.length(); i++)   start=accumulate(doNDArray,i,start);
  return start;
}
