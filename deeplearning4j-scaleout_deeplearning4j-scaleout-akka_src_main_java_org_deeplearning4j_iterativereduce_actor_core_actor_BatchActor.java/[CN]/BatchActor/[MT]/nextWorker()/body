{
  boolean sent=false;
  while (!sent) {
    for (    String s : stateTracker.workers()) {
      if (!stateTracker.workerData().contains(s) && stateTracker.jobFor(s) == null) {
        return s;
      }
    }
  }
  return null;
}
