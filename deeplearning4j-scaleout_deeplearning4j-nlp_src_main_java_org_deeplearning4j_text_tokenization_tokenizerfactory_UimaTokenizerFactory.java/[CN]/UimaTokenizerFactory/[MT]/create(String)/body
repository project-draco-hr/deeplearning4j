{
  if (toTokenize == null || toTokenize.isEmpty())   throw new IllegalArgumentException("Unable to proceed; on sentence to tokenize");
  return new UimaTokenizer(toTokenize,uimaResource,checkForLabel);
}
