{
  this.N=N;
  this.n_in=n_in;
  this.n_out=n_out;
  if (rng == null)   this.rng=new Random(1234);
 else   this.rng=rng;
  if (W == null)   this.W=DoubleMatrix.randn(n_out,n_in);
 else   this.W=W;
  if (b == null)   this.b=DoubleMatrix.zeros(n_out);
 else   this.b=b;
}
