{
  NDArray n=NDArray.wrap(v);
  if (n.isVector() && n.length != rows())   throw new IllegalArgumentException("Unable to put row, mis matched columns");
  NDArray column=getColumn(c);
  for (int i=0; i < v.length; i++)   column.put(i,v.get(i));
}
