{
  if (shape.length == 2) {
    NDArray ret=new NDArray(data,new int[]{shape[0]},new int[]{stride[0]},offset + c,ordering);
    return ret;
  }
 else   throw new IllegalArgumentException("Unable to getFromOrigin column of non 2d matrix");
}
