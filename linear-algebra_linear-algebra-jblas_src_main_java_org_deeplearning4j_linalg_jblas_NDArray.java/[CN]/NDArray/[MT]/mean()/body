{
  if (isVector()) {
    double ret=0.0;
    for (int i=0; i < length; i++) {
      ret+=get(i);
    }
    return ret / length();
  }
  return (double)NDArrayUtil.doSliceWise(NDArrayUtil.ScalarOp.MEAN,this).element();
}
