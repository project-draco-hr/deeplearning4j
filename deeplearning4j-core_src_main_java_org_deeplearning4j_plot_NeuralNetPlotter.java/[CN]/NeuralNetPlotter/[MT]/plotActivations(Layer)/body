{
  if (network.input() == null)   throw new IllegalStateException("Unable to plot; missing input");
  INDArray hbiasMean=network.activationMean();
  String data=writeMatrix(hbiasMean);
  renderGraph("hbias",data);
}
