{
  RandomGenerator rng=new MersenneTwister(conf.getLong(SEED));
  BaseNeuralNetwork matrix=new BaseNeuralNetwork.Builder<>().withClazz(conf.getClazzSingle(CLASS)).withRandom(rng).withL2(conf.getDouble(L2)).withMomentum(conf.getDouble(MOMENTUM)).numberOfVisible(conf.getInt(N_IN)).numHidden(conf.getInt(OUT)).build();
  masterResults=new UpdateableSingleImpl(matrix);
}
