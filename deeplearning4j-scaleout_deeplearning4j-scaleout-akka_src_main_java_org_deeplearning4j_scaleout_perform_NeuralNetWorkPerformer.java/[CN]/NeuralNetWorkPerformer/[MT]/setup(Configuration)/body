{
  NeuralNetConfiguration conf2=NeuralNetConfiguration.fromJson(conf.get(NEURAL_NET_CONF));
  try {
    this.neuralNetwork=new BasePretrainNetwork.Builder<>().asType((Class<BasePretrainNetwork>)Class.forName(conf.get(CLASS))).configure(conf2).build();
  }
 catch (  ClassNotFoundException e) {
    e.printStackTrace();
  }
}
