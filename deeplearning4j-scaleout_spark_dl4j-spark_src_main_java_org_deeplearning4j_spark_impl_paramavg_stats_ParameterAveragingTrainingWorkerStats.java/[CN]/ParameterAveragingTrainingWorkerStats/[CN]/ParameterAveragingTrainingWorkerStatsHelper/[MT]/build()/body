{
  return new ParameterAveragingTrainingWorkerStats(Collections.singletonList((EventStats)new BaseEventStats(broadcastStartTime,broadcastEndTime - broadcastStartTime)),Collections.singletonList((EventStats)new BaseEventStats(broadcastEndTime,initEndTime - broadcastEndTime)),fitTimes);
}
