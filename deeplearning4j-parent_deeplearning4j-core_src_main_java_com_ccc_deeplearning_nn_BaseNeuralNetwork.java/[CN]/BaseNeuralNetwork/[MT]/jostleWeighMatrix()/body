{
  double a=fanIn();
  UniformRealDistribution u=new UniformRealDistribution(rng,-a,a);
  DoubleMatrix W=DoubleMatrix.zeros(nVisible,nHidden);
  for (int i=0; i < this.W.rows; i++)   W.putRow(i,new DoubleMatrix(u.sample(this.W.columns)));
}
