{
  double[] ret=new double[labels.size() + 1];
  for (int i=0; i < labels.size() + 1; i++) {
    ret[i]=0;
  }
  int idx=labels.indexOf(label);
  if (idx < 0) {
    return new double[]{1.0,0.0};
  }
 else   return new double[]{0.0,1.0};
}
