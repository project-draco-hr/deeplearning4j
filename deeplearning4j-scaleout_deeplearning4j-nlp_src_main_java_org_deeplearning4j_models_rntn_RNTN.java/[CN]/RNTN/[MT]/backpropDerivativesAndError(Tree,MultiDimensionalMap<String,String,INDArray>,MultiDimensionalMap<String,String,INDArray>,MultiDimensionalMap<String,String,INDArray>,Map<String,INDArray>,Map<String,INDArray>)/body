{
  if (wordVectorD == null)   throw new IllegalStateException("eh?");
  INDArray delta=Nd4j.create(numHidden,1);
  backpropDerivativesAndError(tree,binaryTD,binaryCD,binaryINDArrayTD,unaryCD,wordVectorD,delta);
}
