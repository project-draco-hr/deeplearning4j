{
  int end=curr + num;
  List<DataSet> r=new ArrayList<DataSet>();
  if (end >= list.size())   end=list.size();
  for (; curr < end; curr++) {
    r.add(list.get(curr));
  }
  DataSet d=DataSet.merge(r);
  return d;
}
