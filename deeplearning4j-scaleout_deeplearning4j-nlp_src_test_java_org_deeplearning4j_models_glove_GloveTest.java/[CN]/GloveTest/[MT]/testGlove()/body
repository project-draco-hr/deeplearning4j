{
  glove=new Glove.Builder().iterate(iter).symmetric(true).shuffle(true).minWordFrequency(1).iterations(10).learningRate(0.1).layerSize(300).build();
  glove.fit();
  Collection<String> words=glove.wordsNearest("day",20);
  assertTrue(words.contains("week"));
}
