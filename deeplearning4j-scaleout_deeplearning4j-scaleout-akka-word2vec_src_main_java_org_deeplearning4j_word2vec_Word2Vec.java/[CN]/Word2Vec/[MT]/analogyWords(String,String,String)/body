{
  TreeSet<VocabWord> analogies=this.analogy(w1,w2,w3);
  List<String> ret=new ArrayList<String>();
  for (  VocabWord w : analogies)   ret.add(wordIndex.get(w.getIndex()).toString());
  return ret;
}
