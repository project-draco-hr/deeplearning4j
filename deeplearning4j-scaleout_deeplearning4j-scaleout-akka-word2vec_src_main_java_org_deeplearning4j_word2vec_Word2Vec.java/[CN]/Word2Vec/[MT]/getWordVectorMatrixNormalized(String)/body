{
  int i=this.wordIndex.indexOf(word);
  if (oob == null)   oob=new float[layerSize];
  if (i < 0)   return FloatMatrix.zeros(syn0.getRow(0).columns);
  FloatMatrix r=syn0.getRow(i);
  return r.div((SimpleBlas.nrm2(r)));
}
