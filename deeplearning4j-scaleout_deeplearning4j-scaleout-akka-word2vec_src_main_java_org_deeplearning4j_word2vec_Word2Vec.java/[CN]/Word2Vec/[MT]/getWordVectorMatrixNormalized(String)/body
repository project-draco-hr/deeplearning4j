{
  int i=this.cache.indexOf(word);
  if (oob == null)   oob=new float[layerSize];
  if (i < 0)   return NDArrays.zeros(layerSize);
  INDArray r=cache.vector(word);
  return r.div((NDArrays.getBlasWrapper().nrm2(r)));
}
