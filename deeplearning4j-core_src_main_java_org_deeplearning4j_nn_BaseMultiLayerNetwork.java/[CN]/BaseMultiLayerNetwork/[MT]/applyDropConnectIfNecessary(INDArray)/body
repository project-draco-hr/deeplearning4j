{
  if (useDropConnect) {
    INDArray mask=Sampling.binomial(NDArrays.valueArrayOf(input.rows(),input.columns(),0.5),1,defaultConfiguration.getRng());
    input.muli(mask);
    if (defaultConfiguration.getL2() > 0)     input.muli(defaultConfiguration.getL2());
  }
}
