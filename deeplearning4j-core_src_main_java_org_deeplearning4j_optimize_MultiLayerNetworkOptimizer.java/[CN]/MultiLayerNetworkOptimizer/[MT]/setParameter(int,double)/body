{
  if (index >= network.getOutputLayer().getW().length) {
    int i=index - network.getOutputLayer().getB().length;
    network.getOutputLayer().getB().put(i,value);
  }
 else   network.getOutputLayer().getW().put(index,value);
}
