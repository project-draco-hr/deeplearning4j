{
  GloveWeightLookupTable ret=new GloveWeightLookupTable.Builder().cache(vocab).vectorLength(vectorLength).build();
  INDArray syn0=Nd4j.readTxt(is," ");
  ret.setSyn0(syn0);
  ret.resetWeights(false);
  return ret;
}
