{
  int result=17;
  result=31 * result + (directed ? 1 : 0);
  result=31 * result + from;
  result=31 * result + to;
  result=31 * result + (value == null ? 0 : value.hashCode());
  return result;
}
