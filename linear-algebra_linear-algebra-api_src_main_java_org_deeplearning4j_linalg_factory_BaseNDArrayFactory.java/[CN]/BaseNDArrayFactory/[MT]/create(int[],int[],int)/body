{
  if (dtype.equals("double"))   return create(new double[ArrayUtil.prod(shape)],shape,stride,offset);
  if (dtype.equals("float"))   return create(new float[ArrayUtil.prod(shape)],shape,stride,offset);
  throw new IllegalStateException("Illegal data type " + dtype);
}
