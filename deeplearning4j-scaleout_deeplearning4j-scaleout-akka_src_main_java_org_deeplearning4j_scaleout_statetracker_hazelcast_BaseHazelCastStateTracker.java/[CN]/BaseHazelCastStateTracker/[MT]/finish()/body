{
  try {
    if (getCurrent() != null) {
      cachedCurrent=getCurrent();
    }
    done.set(true);
    updateSaver().cleanup();
    h.shutdown();
  }
 catch (  Exception e) {
    log.warn("Hazelcast already shutdown...done() being called is pointless");
  }
}
