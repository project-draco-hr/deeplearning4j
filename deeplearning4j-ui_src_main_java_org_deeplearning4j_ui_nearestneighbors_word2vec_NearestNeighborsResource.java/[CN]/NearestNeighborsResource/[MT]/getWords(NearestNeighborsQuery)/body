{
  Collection<String> nearestNeighors=vectors.wordsNearest(query.getWord(),query.getNumWords());
  Map<String,Double> map=new HashedMap();
  for (  String s : nearestNeighors)   map.put(s,0.0);
  return Response.ok(map).build();
}
