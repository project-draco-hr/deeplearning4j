{
  if (index >= network.W.length) {
    if (index >= network.vBias.length) {
      return network.hBias.get(index);
    }
 else     return network.vBias.get(index);
  }
  return network.W.get(index);
}
