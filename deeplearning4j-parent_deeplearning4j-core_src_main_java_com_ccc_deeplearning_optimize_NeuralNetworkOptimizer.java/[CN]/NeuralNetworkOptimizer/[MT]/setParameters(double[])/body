{
  int idx=0;
  for (int i=0; i < network.W.length; i++)   network.W.put(i,params[idx++]);
  for (int i=0; i < network.vBias.length; i++)   network.vBias.put(i,params[idx++]);
  for (int i=0; i < network.hBias.length; i++)   network.hBias.put(i,params[idx++]);
}
