{
  boolean foundWork=false;
  while (!foundWork) {
    for (    WorkerState state : workers.values()) {
      if (state.isAvailable()) {
        foundWork=true;
        return state;
      }
    }
  }
  return null;
}
