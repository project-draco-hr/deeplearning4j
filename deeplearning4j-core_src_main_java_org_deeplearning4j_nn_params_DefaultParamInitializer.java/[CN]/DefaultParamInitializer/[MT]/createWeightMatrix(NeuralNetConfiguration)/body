{
  Distribution dist=Distributions.createDistribution(conf.getDist());
  INDArray ret=WeightInitUtil.initWeights(conf.getNOut(),conf.getNOut(),conf.getWeightInit(),dist);
  ret.data().persist();
  return ret;
}
