{
  setInput(data.dup());
  if (layerWiseConfigurations.isPretrain()) {
    pretrain(getInput());
    finetune(labels);
  }
  if (layerWiseConfigurations.isBackprop()) {
    setLabels(labels);
    backprop();
  }
}
