{
  for (  Layer layer : layers) {
    if (!(layer instanceof SubsamplingLayer)) {
      layer.conf().getLayer().setLearningRate(layer.conf().getLayer().getLearningRate() * (layer.conf().getLayer().getLrScoreBasedDecay() + Nd4j.EPS_THRESHOLD));
    }
  }
}
