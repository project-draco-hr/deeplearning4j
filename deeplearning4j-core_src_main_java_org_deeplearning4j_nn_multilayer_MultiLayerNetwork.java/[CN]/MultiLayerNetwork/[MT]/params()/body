{
  List<INDArray> params=new ArrayList<>();
  for (  Layer layer : getLayers())   if (!(layer instanceof SubsamplingLayer)) {
    params.add(layer.params());
  }
  return Nd4j.toFlattened(params);
}
