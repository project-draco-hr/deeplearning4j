{
  IComplexNDArray inputC=NDArrays.createComplex(transform);
  if (inputC.isVector())   return new VectorFFT(numElements).apply(inputC);
 else {
    return rawfft(inputC,numElements,dimension);
  }
}
