{
  if (cache) {
    indexManager.execute(new Runnable(){
      public void run(){
        try {
          if (dir == null)           dir=FSDirectory.open(new File("word2vec-index"));
          if (writer == null)           writer=new IndexWriter(dir,iwc);
          writer.forceMerge(1);
          writer.commit();
          initReader();
          numDocs=reader.numDocs();
        }
 catch (        IOException e) {
          e.printStackTrace();
        }
      }
    }
);
  }
 else {
    try {
      writer.forceMerge(1);
      writer.commit();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    initReader();
    numDocs=reader.numDocs();
  }
}
