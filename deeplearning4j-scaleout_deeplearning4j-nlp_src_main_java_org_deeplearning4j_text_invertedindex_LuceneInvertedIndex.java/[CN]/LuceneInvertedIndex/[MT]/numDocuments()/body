{
  if (numDocs > 0)   return numDocs;
  int ret;
  try {
    IndexWriter writer=getWriter();
    initReader(writer);
    ret=reader.numDocs();
  }
 catch (  Exception e) {
    return 0;
  }
  return ret;
}
