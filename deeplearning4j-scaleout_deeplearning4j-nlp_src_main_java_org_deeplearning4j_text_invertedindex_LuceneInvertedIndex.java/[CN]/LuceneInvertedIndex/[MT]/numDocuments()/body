{
  if (numDocs > 0)   return numDocs;
  int ret;
  try {
    IndexWriter writer=getWriter();
    initReader(writer);
    ret=reader.numDocs();
    writer.close();
  }
 catch (  Exception e) {
    return 0;
  }
  return ret;
}
