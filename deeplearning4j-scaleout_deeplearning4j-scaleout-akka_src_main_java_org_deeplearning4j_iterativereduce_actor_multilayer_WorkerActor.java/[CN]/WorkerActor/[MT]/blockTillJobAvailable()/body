{
  Job j=null;
  int numRetries=0;
  int maxRetries=10;
  while ((j=tracker.jobFor(id)) == null) {
    Thread.sleep(2 * numRetries);
    numRetries++;
    if (numRetries >= maxRetries)     break;
  }
  if (j != null) {
    log.info("Assigning job for worker " + id);
    currentJob=j;
  }
}
