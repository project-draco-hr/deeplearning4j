{
  if (getCurrent() != null) {
    getCurrent().setDone(true);
    log.info("Finished work " + id);
    mediator.tell(new DistributedPubSubMediator.Publish(MasterActor.MASTER,getCurrent()),getSelf());
  }
  clearCurrentJob();
}
