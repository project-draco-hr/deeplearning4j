{
  getIfNotExists();
  LabelAwareSentenceIterator iter=new LabelAwareFileSentenceIterator(reutersRootDir);
  List<String> labels=Arrays.asList("label1","label2");
  TokenizerFactory tokenizerFactory=new UimaTokenizerFactory();
  if (tfidf)   this.textVectorizer=new TfidfVectorizer(iter,tokenizerFactory,labels);
 else   this.textVectorizer=new BagOfWordsVectorizer(iter,tokenizerFactory,labels);
  load=this.textVectorizer.vectorize();
}
