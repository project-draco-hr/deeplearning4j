{
  if (!isSquare(nums))   throw new IllegalStateException("WTF IS THIS");
  DoubleMatrix ret=new DoubleMatrix(nums.length,nums[0].length);
  for (int i=0; i < nums.length; i++)   for (int j=0; j < nums[i].length; j++)   ret.put(i,j,nums[i][j]);
  return ret;
}
