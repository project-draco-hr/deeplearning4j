{
  ComplexNDArray inputC=new ComplexNDArray(transform);
  if (inputC.isVector())   return new VectorFFT(inputC.length).apply(inputC);
 else {
    return rawifft(inputC,numElements,inputC.shape().length - 1);
  }
}
