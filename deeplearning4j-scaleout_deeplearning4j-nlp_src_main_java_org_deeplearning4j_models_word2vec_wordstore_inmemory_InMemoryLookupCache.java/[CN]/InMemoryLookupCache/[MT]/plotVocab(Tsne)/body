{
  try {
    List<String> plot=new ArrayList<>();
    for (    String s : words()) {
      plot.add(s);
    }
    tsne.plot(syn0,2,syn0.shape()[1],plot);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
