{
  NeuralNetConfiguration conf=new NeuralNetConfiguration.Builder().build();
  String json=conf.toJson();
  NeuralNetConfiguration read=NeuralNetConfiguration.fromJson(json);
  assertEquals(conf,read);
}
