{
  super(null,batchSize,width * height * channels,CifarLoader.NUM_LABELS);
  this.loader=new CifarLoader();
  this.inputStream=loader.getInputStream();
  this.numExamples=numExamples > totalExamples ? totalExamples : numExamples;
}
