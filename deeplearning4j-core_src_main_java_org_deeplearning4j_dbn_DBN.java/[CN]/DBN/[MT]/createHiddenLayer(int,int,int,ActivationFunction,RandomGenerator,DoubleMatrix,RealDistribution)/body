{
  if (hiddenUnit == RBM.HiddenUnit.RECTIFIED)   return new RectifiedLinearHiddenLayer.Builder().nIn(nIn).nOut(nOut).withActivation(activation).withRng(rng).withInput(layerInput).dist(dist).build();
 else   return super.createHiddenLayer(index,nIn,nOut,activation,rng,layerInput,dist);
}
