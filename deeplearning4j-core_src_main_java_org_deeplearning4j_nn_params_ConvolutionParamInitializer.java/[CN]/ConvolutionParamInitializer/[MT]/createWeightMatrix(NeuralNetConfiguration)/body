{
  Distribution dist=Distributions.createDistribution(conf.getLayer().getDist());
  return WeightInitUtil.initWeights(Ints.concat(new int[]{conf.getNOut(),conf.getNIn()},((org.deeplearning4j.nn.conf.layers.ConvolutionLayer)conf.getLayer()).getKernelSize()),conf.getLayer().getWeightInit(),dist);
}
