{
  this.words=new ArrayList<>(words);
  Collections.sort(this.words,new Comparator<VocabWord>(){
    @Override public int compare(    VocabWord o1,    VocabWord o2){
      return Double.compare(o2.getWordFrequency(),o1.getWordFrequency());
    }
  }
);
}
