{
  if (arg0 instanceof ResetMessage) {
    iter.reset();
    numTimesReset++;
  }
 else   if (iter.hasNext()) {
    mediator.tell(new DistributedPubSubMediator.Publish(MasterActor.RESULT,iter.next()),mediator);
  }
 else   unhandled(arg0);
}
