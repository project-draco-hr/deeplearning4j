{
  coOccurrences=new AbstractCoOccurrences.Builder<T>().symmetric(false).windowSize(configuration.getWindow()).iterate(iterator).workers(Runtime.getRuntime().availableProcessors()).vocabCache(vocabCache).build();
  coOccurrences.fit();
}
