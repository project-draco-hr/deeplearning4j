{
  NeuralNetConfiguration conf=new NeuralNetConfiguration.Builder().constrainGradientToUnitNorm(true).seed(123).layer(new SubsamplingLayer.Builder(pooling).activation("relu").build()).build();
  return LayerFactories.getFactory(new SubsamplingLayer()).create(conf);
}
