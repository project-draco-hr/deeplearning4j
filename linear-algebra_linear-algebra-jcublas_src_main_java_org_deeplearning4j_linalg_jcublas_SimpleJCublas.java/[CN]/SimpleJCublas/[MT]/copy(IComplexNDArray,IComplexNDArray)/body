{
  Pointer X=new Pointer();
  Pointer Y=new Pointer();
  ;
  twoPointersV(X,Y,x,y,Sizeof.FLOAT);
  JCublas.cublasZcopy(x.length(),X,1,Y,1);
  gvi(Y,y,Sizeof.FLOAT);
  JCublas.cublasFree(X);
  JCublas.cublasFree(Y);
}
