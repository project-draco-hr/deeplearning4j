{
  Pointer dA=new Pointer();
  onePointerVi(dA,x,Sizeof.FLOAT);
  JCublas.cublasZscal(x.length(),jcuda.cuDoubleComplex.cuCmplx(alpha.realComponent(),alpha.imaginaryComponent()),dA,2);
  gvi(dA,x,Sizeof.FLOAT);
  JCublas.cublasFree(dA);
  return x;
}
