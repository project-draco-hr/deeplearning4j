{
  Pointer X=pointerFor(x);
  Pointer Y=pointerFor(y);
  twoPointers(X,Y,x,y,Sizeof.FLOAT);
  JCublas.cublasDcopy(x.length(),X,1,Y,1);
  gv(Y,y,Sizeof.FLOAT);
  JCublas.cublasFree(X);
  JCublas.cublasFree(Y);
}
