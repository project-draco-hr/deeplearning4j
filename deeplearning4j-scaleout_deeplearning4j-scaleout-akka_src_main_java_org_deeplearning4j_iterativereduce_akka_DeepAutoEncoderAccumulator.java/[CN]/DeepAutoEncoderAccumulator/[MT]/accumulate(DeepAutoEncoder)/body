{
  if (averaged == null)   this.averaged=deepAutoEncoder;
 else {
    averaged.getDecoder().merge(deepAutoEncoder.getDecoder(),numWorkers);
    averaged.getEncoder().merge(deepAutoEncoder.getEncoder(),numWorkers);
  }
}
