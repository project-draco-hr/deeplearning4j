{
  if (stateTracker != null) {
    stateTracker.finish();
    stateTracker.shutdown();
    distributed.shutdown();
  }
  if (new File("model-saver").exists())   new File("model-saver").delete();
  Thread.sleep(10000);
}
