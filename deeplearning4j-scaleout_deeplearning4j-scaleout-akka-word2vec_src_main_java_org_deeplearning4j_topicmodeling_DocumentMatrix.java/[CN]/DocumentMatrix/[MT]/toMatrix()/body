{
  if (initializedWithCounter) {
    DoubleMatrix ret=new DoubleMatrix(words.size());
    int count=0;
    for (    String s : words.keySet())     ret.put(count++,docWords.getCount(s));
    return ret;
  }
 else {
    DoubleMatrix ret=new DoubleMatrix(words.size());
    int count=0;
    for (    String s : words.keySet())     ret.put(count++,words.getCount(s));
    return ret;
  }
}
