{
  @SuppressWarnings("unchecked") List<String> list=FileUtils.readLines(file);
  Word2Vec vec=new Word2Vec();
  vec.buildVocab(list);
  for (  String sentence : list)   vec.addSentence(sentence);
  vec.train();
  return vec;
}
