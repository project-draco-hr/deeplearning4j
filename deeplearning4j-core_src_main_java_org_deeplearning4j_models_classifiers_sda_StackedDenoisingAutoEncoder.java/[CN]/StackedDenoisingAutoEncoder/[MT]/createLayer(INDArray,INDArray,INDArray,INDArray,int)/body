{
  DenoisingAutoEncoder ret=new DenoisingAutoEncoder.Builder().configure(layerWiseConfigurations.getConf(index)).withInput(input).withWeights(W).withHBias(hbias).withVisibleBias(vBias).build();
  return ret;
}
