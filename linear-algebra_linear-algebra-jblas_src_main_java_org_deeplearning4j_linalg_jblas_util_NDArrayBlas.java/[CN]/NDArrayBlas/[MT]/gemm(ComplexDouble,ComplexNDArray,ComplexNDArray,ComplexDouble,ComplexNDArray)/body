{
  int aOffset=a.offset();
  int bOffset=b.offset();
  int cOffset=c.offset();
  double[] aData=a.data();
  double[] bData=b.data();
  double[] cData=c.data();
  NativeBlas.zgemm('N','N',c.rows(),c.columns(),a.columns(),alpha,aData,aOffset,a.rows(),bData,bOffset,b.rows(),beta,cData,cOffset,c.rows());
  return c;
}
