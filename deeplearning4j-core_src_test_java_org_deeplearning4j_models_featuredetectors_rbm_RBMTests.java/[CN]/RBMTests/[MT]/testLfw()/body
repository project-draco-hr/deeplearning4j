{
  LFWDataSetIterator iter=new LFWDataSetIterator(10,10,28,28);
  DataSet d=iter.next();
  d.normalizeZeroMeanZeroUnitVariance();
  int nOut=600;
  RandomGenerator rng=new MersenneTwister(123);
  NeuralNetConfiguration conf=new NeuralNetConfiguration.Builder().weightInit(WeightInit.VI).dropOut(0.3f).hiddenUnit(RBM.HiddenUnit.RECTIFIED).visibleUnit(RBM.VisibleUnit.GAUSSIAN).lossFunction(LossFunctions.LossFunction.RECONSTRUCTION_CROSSENTROPY).rng(rng).learningRate(1e-1f).nIn(d.numInputs()).nOut(nOut).build();
  LayerFactory layerFactory=new DefaultLayerFactory(RBM.class);
  RBM rbm=layerFactory.create(conf);
  rbm.fit(d.getFeatureMatrix());
}
